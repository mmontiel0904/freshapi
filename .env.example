# Database Configuration
DATABASE_URL=postgresql://freshapi_user:freshapi_password@localhost:5432/freshapi_db

# Server Configuration
HOST=127.0.0.1
PORT=8080

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION_HOURS=24

# Email Configuration (Resend)
RESEND_API_KEY=your-resend-api-key-here
FROM_EMAIL=noreply@freshapi.dev

# Frontend URL for email links
FRONTEND_URL=http://localhost:5173

# Environment
RUST_LOG=debug
ENVIRONMENT=development

# CORS Configuration
# For development - accept any origin (DANGEROUS in production):
# CORS_ALLOWED_ORIGINS=*
# For production - specific domains only:
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173
# For production example:
# CORS_ALLOWED_ORIGINS=https://myapp.com,https://www.myapp.com

# Admin User Seeding (only set these for initial setup, then remove)
# ADMIN_EMAIL=admin@freshapi.dev
# ADMIN_PASSWORD=change-this-secure-password
# ADMIN_FIRST_NAME=Admin
# ADMIN_LAST_NAME=User